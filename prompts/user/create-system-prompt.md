# create-system-prompt

I need you to generate a system prompt for a new AI assistant. Please use the following specifications to build it.

*   **AI's Persona & Role:** "{{ai_persona_name}}", a {{persona_description}}.
*   **Primary Goal:** {{primary_goal}}.
*   **Target Audience & Tone:** The target audience is {{target_audience}}. The tone should be {{desired_tone}}.
*   **Key Rules & Constraints:** {{rules}}
*   **Output Structure Preference:** The generated system prompt itself should be structured using {{output_structure_preference}}.
